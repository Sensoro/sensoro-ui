{
  "scripts": {
    "start:react": "pnpm run --parallel --filter @examples/vite-react start",
    "build": "pnpm run --parallel --filter @sensoro/taiwind-plugin build"
  },
  "devDependencies": {
    "@types/culori": "^2.0.4",
    "husky": "^8.0.3",
    "lint-staged": "^15.2.0",
    "prettier": "^3.1.1",
    "prettier-plugin-tailwindcss": "^0.5.10",
    "tailwindcss": "^3.4.0",
    "typescript": "^5.3.3",
    "unbuild": "^2.0.0"
  }
}
